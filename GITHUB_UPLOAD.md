# GitHub Upload Guide

Suggested repository name:

```text
oauth-protocol-doctor
```

Suggested description:

```text
Diagnose Windows URL protocol handlers used by OAuth desktop callbacks.
```

Suggested topics:

```text
windows, oauth, protocol-handler, powershell, diagnostics, electron
```

Suggested first commit message:

```text
Initial OAuth protocol diagnostics CLI
```

Suggested README pin note:

```text
A small Windows diagnostic tool for OAuth callback protocol issues, built after debugging a real Codex/Gmail connector failure.
```
